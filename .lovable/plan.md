Проверил GitHub через подключённый коннектор:

- Я вижу репозиторий `alexgood11121-create/Gotti-stuff`, но в нём пока нет запусков Actions.
- Файл `.github/workflows/android-build.yml` в этом репозитории сейчас не найден, поэтому ошибка «в 1-й строчке» скорее всего из другого репозитория или из файла, который ещё не синхронизировался туда.
- Старый репозиторий `alexgood11121-create/gotti-stuff-sales-hub` через GitHub API сейчас возвращает `404`, значит у подключённого коннектора нет доступа к нему или репозиторий называется иначе.

План исправления:

1. Найти правильный репозиторий
   - Проверю точный repo, где лежит workflow и где GitHub показывает красную ошибку.
   - Если это `Gotti-stuff`, добавлю туда корректный `.github/workflows/android-build.yml`.
   - Если это `gotti-stuff-sales-hub`, нужно дать GitHub-коннектору доступ именно к этому репозиторию или прислать точный `owner/repo`.

2. Исправить ошибку в первой строке workflow
   - Перепишу workflow в валидный YAML с правильной первой строкой `name: Android Build`.
   - Уберу проблемные шаги, которые раньше падали из-за TypeScript/Capacitor CLI.
   - Оставлю стабильный путь: установка Node/Java/Android SDK → установка `@capacitor/android` → Gradle build → загрузка APK artifact → публикация APK в Release.

3. Сделать скачивание APK рабочим
   - Настрою загрузку APK как GitHub Actions artifact, чтобы его можно было скачать даже если Release не создался.
   - Настрою Release `latest` с файлом `gotti-stuff-latest.apk`.
   - После успешного запуска дам только проверенную ссылку, которая реально существует.

4. Что нужно от тебя
   - Открой страницу с ошибкой GitHub Actions и пришли сверху название репозитория, как написано в левом верхнем углу: `owner/repo`.
   - Либо дай доступ GitHub-коннектору к тому репозиторию, где сейчас красная ошибка.

После подтверждения я внесу правки в workflow и проверю по GitHub API, что файл и запуск появились.